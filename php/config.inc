<?php

   define('REPO_ACTIONS', ".github");
   define('LOGFILE', '');           // Incluya una ruta para escribir en archivo
   define('DEV_MODE', false);       // true: no necesita secret (util para depurar con wget o curl)
