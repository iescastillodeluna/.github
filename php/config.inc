<?php

   define('REPO_ACTIONS', "classroom-actions");
   define('LOGFILE', '');           // Si no está definido un archivo, no hay registro.
   #define('LOGFILE', '/tmp/github_event.log');
   define('DEV_MODE', false);        // true: no necesita secret (util para depurar con wget o curl)
